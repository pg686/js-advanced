function encodeAndDecodeMessages() {
    console.log('TODO...')
    let encodeBtn = document.querySelectorAll('#main DIV:nth-child(1) button');
    let decodeBtn = document.querySelectorAll('#main DIV:nth-child(2) button');
    encodeBtn.adde
}